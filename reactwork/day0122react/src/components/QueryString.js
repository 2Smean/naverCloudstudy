import React from 'react';

const QueryString = () => {
    return (
        <div>
            <h3 className='alert alert-warning'>QueryString</h3>
        </div>
    );
};

export default QueryString;